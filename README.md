# flask-pygood
A wrapper that allows you to use Pygood easily with Flask endpoints

## Setting up the source code
```
virtualenv-3.3 env
source env/bin/activate
pip install -r requirements.txt
python3 -m flask_pygood.test.demo
```
